# Vector search
faiss-cpu
sentence-transformers
numpy

# BM25 retrieval
rank-bm25

# AWS Bedrock / S3
boto3

# Mistral (OpenAI-compatible API)
openai

# PDF generation
reportlab

# Import checks / browser stack
PyMuPDF
greenlet
playwright

# Token counting
tiktoken

# Environment variables
python-dotenv

# Scheduled jobs (weekly digest notifications)
apscheduler

# JSON Schema validation (optional but recommended)
jsonschema

# HTTP API server
fastapi
uvicorn[standard]
python-multipart

# Authentication
python-jose[cryptography]
passlib[bcrypt]
pydantic[email]

# MongoDB async driver
motor
pymongo
